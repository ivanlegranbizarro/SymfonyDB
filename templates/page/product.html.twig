{% extends 'base.html.twig' %}

{% block title %}
  Product detail
{% endblock %}

{% block body %}
  <h1 class="mb-4">Product {{ product.name }}</h1>
  <p>
    <strong>Código:</strong>#{{ product.metadata.code }}
  </p>
  <p>{{ product.summary }}</p>
  <hr />
  <p>{{ product.metadata.content }}</p>
  <p class="text-muted">
    {{ product.comments|length }} Comentarios |{% for tag in product.tags %}
      <a href="{{ path('app_tag', { id: tag.id }) }}" class="badge badge-light text-dark text-decoration-none">{{ tag.name }}</a>
    {% endfor %}
  </p>
  <hr />

  <div class="px-4">
    {% for comment in product.comments %}
      <div class="row mb-4">
        <h4 class="col-md-2">ID #{{ comment.id }}</h4>
        <p class="col-md-10">{{ comment.content }}</p>
      </div>
    {% endfor %}
  </div>
{% endblock %}
